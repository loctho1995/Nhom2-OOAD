@model IEnumerable<Common.ViewModels.PhieuDatHangViewModel>

@{
    ViewBag.Title = "Hủy Phiếu Đặt Hàng";
    Layout = "~/Areas/Admin/Views/Layout/HomeLayout.cshtml";
}

<section id="page-content">
    <div class="header-content">
        <h2><i class="fa fa-list-alt"></i>Phiếu Đặt Hàng</h2>
        <div class="breadcrumb-wrapper hidden-xs">
           @using (Html.BeginForm("Delete", "DatHang"))
           {
            <button class="btn btn-sm btn-danger btn-xs btn-push">
                <i class="fa fa-trash"></i> Hủy Phiếu Đặt Hàng
            </button>
           }
        </div>
    </div>
    <div class="body-content animated fadeIn">
        <div class="row">
            <div class="alert alert-danger1 fade in">
                <span><i class="fa fa-bullhorn"></i> Bạn có chắc chắn muốn hủy?</span>
            </div>

            @{Html.RenderAction("ThongTinPhieuDatHang", "DatHang");}
        </div>

    </div>
</section>

<script src="~/Scripts/bootstrap.min.js"></script>